import { TranslationResultEntity } from './translation-result.entity';

describe('TranslationResultEntity', () => {
  it('should create an instance', () => {
    expect(new TranslationResultEntity()).toBeTruthy();
  });
});
